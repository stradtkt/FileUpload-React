{
  "name": "file-upload",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "start-server": "node server/server.js",
    "run-server": "nodemon server/server.js",
    "client": "concurrently \"npm run run-server\" \"npm start\"",
    "express": "^4.17.1",
    "express-fileupload": "^1.1.5",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^4.1.0",
    "nodemon": "^1.19.1"
  }
}
